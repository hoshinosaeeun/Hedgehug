// 임시 예시 코드입니다. 실제로는 Firebase Admin SDK 또는 Expo Notification API 등을 사용합니다.
module.exports = async function sendPushNotification(userId, message) {
  console.log(`알림 전송 [${userId}]: ${message}`);
  // 실제 푸시 알림 전송 로직 구현
};
